<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Roboto&display=swap" rel="stylesheet">
    <title>Fill-In the Brain</title>
  </head>

  <body>
    <% if(user){%>
    <script src="/javascripts/headerFunctions.js" defer></script>
    <%}%>
    <header>
      <nav>
        <h1><a href="/"><img src="/images/Logo-Text.png"></a></h1>
        <form method="GET" action="/modules/search"> 
          <label for = "search"><i class="fa fa-search" style="font-size:24px"></i></label>
          <input id = "search" name = "search" type="text" placeholder="Search Fill-In the Brain..." />
          <input type="submit">
        </form>
        <div>
          <div></div>
          <div>
            <% if(user){ %>
            <a href="/modules/user/<%=user.id%>">Your Modules</a>
            <a href="/modules/create">Create a Module</a>
            <div id="account">
              <img src ="<%=user.avatar%>"/>
              <div id="act-pop-up">
                <ul>
                  <li>
                    <a href="/user/<%=user.id%>">Your Profile</a>
                  </li>
                  <li>
                    <a href="/auth/logout">Log Out</a>
                  </li>
                </ul>
            </div>
            </div>
            <% }else{%>
            <a href="/auth/login">
              <input type="button" value="Log In" />
            </a>
          <% }%>
          </div>
        </div>
      </nav>
    </header>

